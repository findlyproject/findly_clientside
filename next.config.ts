import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {

    domains: ['res.cloudinary.com',"via.placeholder.com","lh3.googleusercontent.com",'i.pinimg.com'], 
  },
  
};

export default nextConfig;
